# 青梧 项目规范

## 产品定位

青梧（Qingwu）是面向普通办公用户的 Windows 办公 Agent 桌面客户端。应用随包内置并自动启动 DeepSeek Harness（dsh）；用户双击即可使用，无需安装 Node、npm、dsh 或打开浏览器。青梧界面是当前默认界面和长期主线；原样保留的 DeepSeek 界面可随时切换，两套界面共享同一引擎与会话数据。

Agent Loop、工具、模型、沙箱与会话持久化由 dsh 提供，青梧不重复实现。办公文档处理依赖 Harness 的 tool / Skill 体系，客户端当前不自建一套处理能力。当前只发布 Windows 安装包，不承诺 macOS、Linux，也不提供多引擎切换。长期方向是可靠的引擎桌面封装、成熟的桌面交互，以及基于 Harness 的办公场景能力。

保持产品与交互克制：不增加非必要功能，不凭空设想未经行业验证的特性；界面与交互优先对齐 Codex、ChatGPT 等成熟桌面标杆，兼顾办公场景的原生能力，如系统默认程序打开和文件管理器定位。

## 技术内容

青梧以 Electron 为桌面壳：主进程管理窗口、引擎启停、通信代理、设置与更新；preload 暴露受限 IPC；React 渲染青梧界面，DeepSeek 界面由 WebContentsView 加载。两套界面连接同一个 dsh 子进程，不另建引擎业务数据。`out/` 和 `dist/` 是生成产物，不手工编辑。

- [技术总览](docs/tech-architecture.md)：当前架构、跨模块关系和共同约束。
- [桌面应用](docs/桌面应用/桌面应用.md)：启停、窗口、双界面、菜单、托盘和更新。
- [dsh 运行与连接](docs/dsh运行与连接/dsh运行与连接.md)：随包运行时、进程清理、鉴权、RPC 和逻辑流。
- [会话与项目](docs/会话与项目/会话与项目.md)：会话、项目、消息流、草稿、审批与队列。
- [设置与模型](docs/设置与模型/设置与模型.md)：应用设置、引擎配置、模型供应商和权限。
- [工作区文件与交付](docs/工作区文件与交付/工作区文件与交付.md)：文件变更、交付物、右侧面板和桌面文件操作。
- [界面与视觉设计](docs/界面与视觉设计/界面与视觉设计.md)：设计原则、色彩 Token、组件交互动线与暗色模式。

技术任务按涉及的模块读取说明并核对代码；涉及整体技术路线或跨模块关系时再读技术总览。不默认通读仓库，也不把旧设计方案当作现行实现。底层能力与通信协议优先复用 dsh，不自造重叠协议或 Agent 能力；引擎接口变化须核对当前依赖与两套界面的影响。

## 项目管理

### 工作原则

- 不要为以后留下技术债，不要给以后留坑；不为短期功能牺牲长期维护。
- 除非用户明确要求，不主动提出"先打临时补丁、以后再清理"式的短期方案；确需短期手段时，必须同时说明长期代价与退出路径，并交用户决定。
- 需求存在实质歧义时先对齐，不自行选择一种理解直接完成交付。

### 工作规范

- 用户已给明确任务时直接处理；需要选择或规划未完成工作、或任务明确指向已有事项时才读 docs/TODO.md。新增事项按「新功能」「Bug 修复」「暂不考虑」归类；完成事项标记状态，发版时归档至 docs/TODO-ARCHIVE.md。
- 修改前先检查 git status 与目标文件差异，只处理本任务范围，保留他人未提交改动。
- 任何会改变项目、依赖、应用或发布版本号的操作，必须先向用户说明拟变更的版本号、影响范围和发布后果，并取得用户明确同意；“更新”“发布”等表述不等于同意 Agent 自行递增、跳过或重写版本号。未经确认，不得修改版本字段、创建/移动/删除 Git tag，或发布对应版本。
- 依赖管理统一使用 npm。技术方案如果有行业最佳实践，尽量采用行业最佳实践。

### 沟通规范

- 用户负责产品目标、使用场景、体验判断和重要取舍；Agent 负责技术可行性判断与全部工程实现。会影响产品体验、支持平台或长期维护的技术路线，由 Agent 讲清影响并提出建议后交用户确认。
- 用户提出的技术方案是讨论输入，不自动视为执行指令；需要确认时，把技术问题转换为可感知的产品效果、使用代价和取舍选项。
- 当需求无法可靠实现，或成本、风险、维护负担明显高于收益时，Agent 必须说明实际机制和用户可见后果。
- 与用户讨论问题时先用场景说明影响（触发操作、实际后果、常见还是少见），再给具体方案；避免堆代码。

### 上下文维护

- 长期项目上下文只有产品定位、技术内容、项目管理三层；发布等具体操作方法使用 Skill，不另设平行说明文件。
- 产品目标或边界变化时更新本文件；技术路线、模块职责、接口或数据归属变化时更新受影响的技术总览或模块说明；任务状态变化时更新 TODO。只改受影响的权威位置，发现过时或冲突表述时直接修正，不累积多份现状。
- 文档根目录（docs/）保持结构精简纯净，严禁单独建 modules/ 这类中间目录，严禁在文档根目录下散落零散问题文档或临时文件。文档根目录下仅保留技术总览（tech-architecture.md）、待办（TODO.md）、归档（TODO-ARCHIVE.md）及各模块专属目录（如 `docs/桌面应用/`、`docs/dsh运行与连接/`、`docs/会话与项目/` 等）。每个模块直接在 docs/ 下独立建文件夹，该模块的主文档、技术设计与问题调查记录全部收纳在模块文件夹内部。
- TODO 主要记录任务、必要范围和状态。进行中的复杂调查可暂存解决任务所需的信息；问题解决后收敛任务记录。已解决且容易因后续修改再次出现的非显然 Bug，将触发条件、根因和必须保持的技术约束写入所属模块说明，不把排障过程或 Git 历史当作长期技术依据。
- 技术文档说明当前实现；动手前仍须核对目标代码与差异。历史归档只用于查找已完成事项，不作为现行技术说明。

### 开发与质量门禁（代码落地后启用）

- 代码任务完成前运行 npm run check（脚本与依赖版本以 package.json 为准）。本条仅约束修改代码的任务；纯提交、归档、文档整理等不改代码的任务不运行检查，门禁由 CI 承担。
- 人工验证保持轻量：仅在能可靠控制界面且改动依赖交互表现时验证；无法可靠控制时记录未验证项，交用户实际体验。

### 提交规范

- 遵循 Conventional Commits：类型前缀 + 中文主题（feat / fix / perf / refactor / docs / test / chore / build / ci）。
- 标题一行说清改了什么：祈使句、不加句号、尽量不超过 50 字符；正文可选，只在需要解释「为什么改」时写，每行不超过 72 字符、控制在两三点以内；一眼能看懂的改动只写标题。
- 版本发布提交固定为 chore: release vX.Y.Z；CHANGELOG.md 由发布流程生成，不手工编辑。
- 版本号从最后一位逐位递增（如 0.1.0 → 0.1.1），最后一位满 9 后进位到中间位（如 0.1.9 → 0.2.0 → 0.2.1），段号不使用两位数；是否包含新功能不改变递增方式；发布前须向用户报出拟用版本号并取得明确同意。
- 对外可见的表述（提交主题会进入公开 CHANGELOG 与 GitHub Releases）使用产品用语：界面统一称「青梧界面」，与「DeepSeek 界面」相对；「自研界面」等工程用语仅用于内部技术文档与代码注释。
- 提交标题与正文用产品语言写用户能看懂的变化：不写「第 N 批」「阶段」「收口」这类内部进度说法，不写只有实现者看得懂的缩写（如「双下拉」「引导页」），提到界面元素时用它在界面上实际显示的名称。
- 提交前核对差异；同一任务涉及的文件若与既有未提交修改交错、无法可靠拆分，直接整文件提交，不为拆分而回退或改写内容；无关文件不纳入。
