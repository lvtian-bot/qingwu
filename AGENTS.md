# 编码规范

## 1. 协作定位

用户负责产品目标、使用场景、体验判断和重要取舍；Agent 负责技术可行性判断与全部工程实现。不产生产品差异的实现细节由 Agent 自主决定；会影响产品体验、支持平台或长期维护的技术路线，由 Agent 说明影响并提出建议，交用户决定。

## 2. 协作规则

- **【结构化回答】** 回答问题时要有清晰的逻辑结构，不需要在回复中呈现超过3行的代码片段，必须用自然语言说清楚问题、方案、逻辑和其他内容。
- **【禁止猜测】** 对于用户提出的问题，不得在没有了解相关信息的情况下通过猜测和推断回答，必须自行读取文件进行分析后再回答。
- 用户提出的技术方案是讨论输入，不自动成为实施要求。Agent 先核对相关代码和约束；需要用户取舍时，说明不同方案对产品效果、使用代价和后续维护的影响。
- 需求有实质歧义时先确认分歧点；任务和边界明确时直接执行。回答项目现状问题前先查相关文件，不凭猜测断言。
- 与用户讨论问题时，先说明触发操作、实际后果及场景常见程度，再给原因、方案和建议，用界面和用户可见的行为来表述（例如"标题栏显示文件名，但编辑区是空白的"），不要只罗列代码位置、函数名、变量名和内部实现概念，。
- 修改用户可见的界面文案（设置项名称、按钮文字、提示语等）前，先向用户展示拟修改内容并取得确认。
- 无法可靠实现，或成本、风险、维护负担明显高于收益时，如实说明原因和用户可见后果。

## 3. 工程实施与验证

- 修改前查看 Git 状态和目标文件差异，识别已有未提交改动；只处理本次任务范围，不覆盖、回退或带入无关改动。

- 依据当前代码、配置和项目约定决定实现方式，优先沿用已有模块职责和成熟做法；涉及接口、数据归属或跨模块行为时，先查清影响，不凭旧文档推断现状。

- 技术方案如果有行业最佳实践，尽量采用行业最佳实践。

- 预构建或打包的生成产物不手工修改。需要变更时修改源码或生成配置，并按项目要求重新生成。

- 不为短期功能留下可避免的技术债；除非用户明确要求，不主动提出“先打临时补丁、以后再清理”式的短期方案。确需采用临时方案时，说明长期代价和退出路径，由用户决定是否接受。

- 代码任务完成后，执行与改动直接相关的检查和测试，以及项目明确规定的质量门禁；命令和范围以项目实际配置为准。纯文档、归档和提交等未改代码的任务，不因本条额外运行代码检查。

- 改动依赖界面交互、设备或运行环境时，按项目约定验证实际行为。无法可靠执行的验证要说明原因和未验证范围；不得把类型检查、构建通过或测试通过说成已经验证运行效果。

## 4. 项目上下文管理

### 4.1 文件结构与职责

以下仅为目录示例，模块名称和文档名称依据项目实际情况确定：

```text
项目根目录/
├─ AGENTS.md
└─ docs/
   ├─ 技术架构.md
   ├─ TODO.md
   ├─ TODO-ARCHIVE.md
   ├─ 编辑器/
   │  ├─ 编辑器架构与选型.md
   │  └─ 编辑器问题记录.md
   ├─ 同步/
   │  ├─ 同步算法设计.md
   │  └─ 同步业务流程.md
   └─ outdated/
```

- **`AGENTS.md`**：保存工作规范、项目信息和项目特有要求。技术概况要说明主要技术组成、关键组件的基本关系、核心数据流和影响多类任务的架构约束，使 Agent 不打开 `docs/` 也能理解项目的技术骨架；同时给出详细技术架构的路径。
- **`docs/技术架构.md`**：详细说明整体技术架构和跨模块设计，列出全部模块及其文档入口。
- **`docs/<模块>/`**：每个模块用独立文件夹保存入口文档、必要的补充文档和问题记录。
- **`docs/TODO.md` 与 `docs/TODO-ARCHIVE.md`**：分别保存当前待办和按版本归档的已完成事项。
- **`docs/outdated/`**：保存退役文档，仅供追溯。
- **项目信息更新**：产品方向、支持平台或功能边界变化时，更新 `AGENTS.md` 中的项目信息及其指向的详细产品说明。

### 4.2 技术架构

- **内容**：技术架构文档应讲清系统分层及各层职责、主要数据流与跨模块调用、模块边界、状态和数据归属、关键设计决策及原因，不能只有技术栈或目录清单。跨模块设计在这里展开；模块内部实现由对应模块文档说明。模块清单与模块文档文件夹一一对应，标明各模块职责、入口文档和主要代码入口。
- **读取**：需要理解整体设计、定位不明确的模块或处理跨模块任务时，先读技术架构，再进入相关模块文档。模块已经明确的任务可直接读模块文档。
- **更新**：整体架构、模块职责、接口或数据归属变化时，更新受影响的技术架构内容和模块文档。若变化影响每次进入项目都应知道的技术骨架，同步更新 `AGENTS.md` 中的技术概况；不把详细架构全文复制进去。

### 4.3 模块文档

- **建立**：新项目先核对已确定的产品能力、代码和配置，按稳定的功能与数据责任划分模块，不按页面或源码文件夹机械划分。确定模块后，在技术架构的模块清单中登记其职责、文件夹、入口文档和主要代码入口，并建立对应文件夹及入口文档。尚未实现的设计须标明状态，不写成当前实现；未确定的模块不预建空文件夹。
- **内容**：入口文档说明模块的职责与边界、当前实现、关键流程、状态归属、接口和必须保持的技术约束。进一步拆解的内容放在同一文件夹内，由入口文档链接；补充文档不能代替入口文档。
- **读取**：任务涉及的模块明确时，读取入口文档和直接相关的补充文档，再核对目标代码与配置。文档与代码不一致时先核查，不直接按旧文档实施。
- **更新**：模块内部流程或技术约束变化时，更新入口文档及直接相关的补充文档；新增、拆分、合并或移除模块时，同步调整技术架构和对应模块文档。只修改承担该信息的原有位置；发现过时或冲突表述时先核查再就地修正，不另建一份现状。没有影响长期说明的修改，不机械改写文档。

### 4.4 待办

- **内容**：`docs/TODO.md` 只记录任务目标、必要范围、状态和问题档案入口；规划、路线图留在所属模块文档。待办是待处理或待评估事项，登记不等于已经决定实施；若实现成本或带来的问题大于收益，须向用户说明并讨论。
- **新增**：新待办按“新功能”“Bug 修复”“暂不考虑”归类，追加到对应分类末尾。
- **读取**：用户给出明确任务时直接处理，不先例行通读 TODO；需要选择或规划未完成工作，或任务明确指向已有事项时才读取。
- **更新**：每次项目任务收尾，都打开 TODO 更新对应事项。完成时标记 `[x]`，按 `✅ YYYY-MM-DD` 记录完成日期，结果说明保持简短；尚未完成时写清进展、阻碍和下一步。复杂问题调查期间，TODO 可暂存接续任务必需的信息，问题解决后收敛为结果与档案链接。版本发布时，将已完成事项从 TODO 移至 `docs/TODO-ARCHIVE.md` 对应版本。

### 4.5 问题记录与技术沉淀

- **建立与内容**：顽固、反复出现或需要跨任务继续调查的问题，在所属模块的 `docs/<模块>/<模块>问题记录.md` 建立档案；跨模块问题归入最相关的模块，没有记录文件时按需建立。档案记录现象、已知事实、尝试方案及结果和下一步；TODO 只保留任务及档案链接。
- **读取与更新**：接续问题时先读档案，避免重复尝试。每尝试一个方案，无论成败都及时回写结果。
- **解决后**：将容易复发的非显然缺陷的触发条件、根因和必须保持的技术约束写入模块入口文档或相关技术说明。调查过程留在问题记录中，不复制到现行技术说明。

### 4.6 历史文档退役

文档完成历史使命后移入 `docs/outdated/`，并修正仍指向旧位置的现行文档入口。`outdated/` 和 Git 历史仅在需要追溯时查阅，不作为当前实现的依据。

## 5. Git 管理规范

- **分支与工作树**：不要在现有工作目录直接切换分支。需要隔离任务时，可以自行创建新的 Git 工作树，并在其中建立任务分支，原工作目录的分支保持不动。确需切换已有工作目录的分支时，先说明原因及对未提交改动的影响，取得用户确认。
- **提交时机**：不要每改一处就提交。同一轮开发中的主要修改、后续修补、相关测试和文档更新，检查完成后集中提交；长期任务可在完成一部分可独立保存的成果时提交。
- **合并范围**：不要求一个提交只有一个主题。几项不完全相关的小改动，只要都已检查、确定保留，且提交说明能如实概括，就可以合并。
- **拆分判断**：较大的独立改动、需要单独审查或回退的改动，以及版本发布，分别提交；不要为了拆分而回退或重做已经交错的修改。
- **提交前核对**：检查工作区和暂存区，确认实际提交内容。用户或其他任务留下的改动不自动带入；用户明确要求提交全部改动时，核对后可以合为一次提交。
- **提交说明**：遵循 Conventional Commits，类型前缀 + 中文主题（feat / fix / perf / refactor / docs / test / chore / build / ci）。标题一行说清改了什么：祈使句、不加句号、尽量不超过 50 字符；正文可选，只在需要解释「为什么改」时写，每行不超过 72 字符、控制在两三点以内；一眼能看懂的改动只写标题。（与模板不一致，待回收裁决）
- **提交说明用语**：用产品语言写用户能看懂的变化——不写“第 N 批”“阶段”“收口”这类内部进度说法，不写只有实现者看得懂的缩写；提到界面元素时用它在界面上实际显示的名称。（与模板不一致，待回收裁决）
- **版本与发布**：改变版本号或发布版本前，先向用户说明拟变更的版本号、影响范围和发布后果，取得明确同意；“更新”“发布”等表述不等于同意 Agent 自行递增、跳过或重写版本号；未经确认，不修改版本字段、不创建/移动/删除 Git tag、不发布版本。版本号经同意后从最后一位逐位递增（如 0.1.0 → 0.1.1），最后一位满 9 后进位到中间位（如 0.1.9 → 0.2.0 → 0.2.1），段号不使用两位数；是否包含新功能不改变递增方式。（与模板不一致，待回收裁决）
# 项目信息与规范

## 1. 项目定位

青梧（Qingwu）是面向普通办公用户的 Windows 办公 Agent 桌面客户端。应用随包内置并自动启动 DeepSeek Harness（dsh）；用户双击即可使用，无需安装 Node、npm、dsh 或打开浏览器。青梧以简洁直观的界面为普通办公用户提供开箱即用的 AI Agent 体验。

Agent Loop、工具、模型、沙箱与会话持久化由 dsh 提供，青梧不重复实现。办公文档处理依赖 Harness 的 tool / Skill 体系，客户端当前不自建一套处理能力。当前只发布 Windows 安装包，不承诺 macOS、Linux，也不提供多引擎切换。长期方向是可靠的引擎桌面封装、成熟的桌面交互，以及基于 Harness 的办公场景能力。

保持产品与交互克制：不增加非必要功能，不凭空设想未经行业验证的特性；界面与交互优先对齐 Codex、ChatGPT 等成熟桌面标杆，兼顾办公场景的原生能力，如系统默认程序打开和文件管理器定位。

## 2. 技术概况

- **主要技术**：Electron 桌面壳，React 渲染界面。主进程管理窗口、引擎启停、通信代理、设置与更新；preload 暴露受限 IPC。`out/` 和 `dist/` 是生成产物，不手工编辑。
- **技术骨架**：主进程连接随包内置的 dsh 子进程，不另建引擎业务数据。底层能力与通信协议优先复用 dsh，不自造重叠协议或 Agent 能力；引擎接口变化须核对当前依赖与界面的影响。
- **详细架构**：[技术总览](docs/tech-architecture.md)，说明当前架构、跨模块关系和共同约束。各模块文档如下：
  - [桌面应用](docs/桌面应用/桌面应用.md)：启停、窗口、菜单、托盘和更新。
  - [dsh 运行与连接](docs/dsh运行与连接/dsh运行与连接.md)：随包运行时、进程清理、鉴权、RPC 和逻辑流。
  - [会话与项目](docs/会话与项目/会话与项目.md)：会话、项目、消息流、草稿、审批与队列。
  - [设置与模型](docs/设置与模型/设置与模型.md)：应用设置、引擎配置、模型供应商和权限。
  - [工作区文件与交付](docs/工作区文件与交付/工作区文件与交付.md)：文件变更、交付物、右侧面板和桌面文件操作。
  - [界面与视觉设计](docs/界面与视觉设计/界面与视觉设计.md)：设计原则、色彩 Token、组件交互动线与暗色模式。
- **读取方式**：技术任务按涉及的模块读取说明并核对代码；涉及整体技术路线或跨模块关系时再读技术总览。不默认通读仓库，也不把旧设计方案当作现行实现。

## 3. 项目开发约束

- 依赖管理统一使用 npm。
- 项目长期上下文只保存在本文件「项目信息与规范」与 docs/ 各模块文档；发布等具体操作方法使用 qingwu-release Skill，不另设平行说明文件。
- docs/ 根目录保持结构精简纯净：仅保留技术总览（tech-architecture.md）、待办（TODO.md）、归档（TODO-ARCHIVE.md）及各模块专属目录（如 `docs/桌面应用/`、`docs/dsh运行与连接/`）。严禁单独建 modules/ 这类中间目录，严禁在文档根目录下散落零散问题文档或临时文件；每个模块直接在 docs/ 下独立建文件夹，模块的主文档、技术设计与问题调查记录全部收纳在模块文件夹内部。

## 4. 开发、构建与验证

- 质量门禁：代码任务完成前运行 `npm run check`（脚本与依赖版本以 package.json 为准）；纯提交、归档、文档整理等不改代码的任务不运行检查，门禁由 CI 承担。
- 人工验证保持轻量：仅在能可靠控制界面且改动依赖交互表现时验证；无法可靠控制时记录未验证项，交用户实际体验。

## 5. 版本与发布

- 版本发布提交固定为 chore: release vX.Y.Z；CHANGELOG.md 由发布流程生成，不手工编辑。
- 对外可见的表述（提交主题会进入公开 CHANGELOG 与 GitHub Releases）使用自然产品用语：统一称「界面」或「UI」，不使用「自研界面」「官方界面」「青梧界面」等相对性称呼。
