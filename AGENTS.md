# 青梧 项目规范

## 项目定位

青梧（Qingwu）是一个办公 Agent 桌面客户端。当前形态是 DeepSeek Harness 的桌面壳：启动应用时自动拉起内置的 @deepseek-ai/dsh，在独立桌面窗口里加载官方 Web UI。产品取舍、能力边界和长期方向以 docs/product-positioning.md 为准；新增产品功能、技术路线调整或文档写作前必须先读该文档。

## 工作纪律

### 核心铁律

- 不要为以后留下技术债，不要给以后留坑；不为短期功能牺牲长期维护。
- 技术实现对齐底层引擎（DSH），UI 与交互设计对齐行业标杆（Codex / ChatGPT）：底层能力与通信协议优先复用与继承 @deepseek-ai/dsh 现有体系，不凭空自造私有协议或重叠轮子；界面排版、视觉质感与操作细节优先对齐成熟桌面标杆，兼顾办公场景的桌面级原生能力（如系统默认程序打开、文件管理器定位）。
- 保持产品与交互克制，坚守奥卡姆剃刀：不增加非必要功能，不凭空设想未经行业验证的特性；如果成熟行业标杆普遍未做或不推荐某项设计，不擅自加入，避免增加用户认知负担与不必要的状态维护复杂度。
- 除非用户明确要求，不主动提出"先打临时补丁、以后再清理"式的短期方案；确需短期手段时，必须同时说明长期代价与退出路径，并交用户决定。
- 需求存在实质歧义时先对齐，不自行选择一种理解直接完成交付。

### 工作规范

- 开始开发前先读 docs/TODO.md。新增事项按「新功能」或「Bug 修复」追加；完成已有事项只更新勾选状态与完成日期，发版时统一归档至 docs/TODO-ARCHIVE.md 对应版本下。
- 修改前先检查 git status 与目标文件差异，只处理本任务范围，保留他人未提交改动。
- 任何会改变项目、依赖、应用或发布版本号的操作，必须先向用户说明拟变更的版本号、影响范围和发布后果，并取得用户明确同意；“更新”“发布”等表述不等于同意 Agent 自行递增、跳过或重写版本号。未经确认，不得修改版本字段、创建/移动/删除 Git tag，或发布对应版本。
- 依赖管理统一使用 npm。技术方案如果有行业最佳实践，尽量采用行业最佳实践。
- 技术路线与架构决策见 docs/tech-architecture.md，相关改动前先读。

### 沟通规范

- 用户负责产品目标、使用场景、体验判断和重要取舍；Agent 负责技术可行性判断与全部工程实现。会影响产品体验、支持平台或长期维护的技术路线，由 Agent 讲清影响并提出建议后交用户确认。
- 用户提出的技术方案是讨论输入，不自动视为执行指令；需要确认时，把技术问题转换为可感知的产品效果、使用代价和取舍选项。
- 当需求无法可靠实现，或成本、风险、维护负担明显高于收益时，Agent 必须说明实际机制和用户可见后果。
- 与用户讨论问题时先用场景说明影响（触发操作、实际后果、常见还是少见），再给具体方案；避免堆代码。

## 项目结构与阅读入口

- src/main/：Electron 主进程，负责窗口、引擎启停、通信代理、应用设置与更新。
- src/preload/：向青梧界面暴露受限的 IPC 接口。
- src/shared/：主进程、preload 与渲染层共用的类型约定。
- src/renderer/：青梧界面、标题栏与更新窗口；官方 DeepSeek 界面由主进程通过 WebContentsView 加载。
- scripts/：随包运行时准备与构建辅助脚本；tests/：关键行为与契约检查。
- docs/：产品定位、当前架构、维护导航、待办与发布说明。
- build/：打包资源；out/ 和 dist/ 为生成产物，不手工编辑。

日常任务先读 docs/TODO.md，再按 docs/maintenance.md 的任务导航读取相关模块；产品与架构相关任务仍须遵守上文的必读要求。文档用于定位，实施前须核对目标代码与当前差异；仅在跨模块改动、重大引擎升级或全面审查时扩大阅读范围，不默认通读整个仓库。模块职责、接口或操作流程变化时，同步更新对应文档；历史实现留在归档中，当前说明以现行代码为准。

## 开发与质量门禁（代码落地后启用）

- 代码任务完成前运行 npm run check（脚本与依赖版本以 package.json 为准）。
- 只改文档时至少运行 Prettier 检查与 git diff --check。
- 人工验证保持轻量：仅在能可靠控制界面且改动依赖交互表现时验证；无法可靠控制时记录未验证项，交用户实际体验。

## 提交规范

- 遵循 Conventional Commits：类型前缀 + 中文主题（feat / fix / perf / refactor / docs / test / chore / build / ci）。
- 标题一行说清改了什么：祈使句、不加句号、尽量不超过 50 字符；正文可选，只在需要解释「为什么改」时写，每行不超过 72 字符、控制在两三点以内；一眼能看懂的改动只写标题。
- 版本发布提交固定为 chore: release vX.Y.Z；CHANGELOG.md 由发布流程生成，不手工编辑。
- 版本号一律从最后一位逐位递增（如 0.1.0 → 0.1.1 → 0.1.2），即使包含新功能也不升中间位；发布前须向用户报出拟用版本号并取得明确同意。
- 对外可见的表述（提交主题会进入公开 CHANGELOG 与 GitHub Releases）使用产品用语：界面统一称「青梧界面」，与「DeepSeek 界面」相对；「自研界面」等工程用语仅用于内部技术文档与代码注释。
- 提交标题与正文用产品语言写用户能看懂的变化：不写「第 N 批」「阶段」「收口」这类内部进度说法，不写只有实现者看得懂的缩写（如「双下拉」「引导页」），提到界面元素时用它在界面上实际显示的名称。
