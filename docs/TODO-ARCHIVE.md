# 青梧 已完成事项归档

按版本归档已完成任务，保持 TODO.md 精炼聚焦。

## v0.1.4 (2026-09-12)

- [x] 会话重命名与操作菜单：侧栏会话条目悬停提供「···」菜单（置顶/重命名/归档），重命名走引擎 session/rename，新标题回填列表与聊天标题栏 ✅ 2026-09-12
- [x] 侧栏三区间划分与置顶：按「置顶」、「项目」、「会话」（无项目日常会话）三区间组织侧栏，支持项目与会话置顶并在本地记忆 ✅ 2026-09-12
- [x] 右侧面板开关按钮固定贴窗口右缘：收起时在会话标题栏右端、展开时移到面板顶栏右端，不再随面板展开被推走，图标对齐官方图形 ✅ 2026-09-12
- [x] 项目内会话默认收起展示，「展开显示 / 收起」按钮按批展开，搜索态不折叠 ✅ 2026-09-12
- [x] 会话历史支持「加载更早」：长会话先载最近一页，更早内容按需前插且视口不跳屏 ✅ 2026-09-12
- [x] 引导页工作区选择条移至输入框下方，统一输入区占位文案与工具行布局 ✅ 2026-09-12
- [x] 修复青梧界面无法粘贴与发送图片：支持剪贴板粘贴与拖拽添加图片、输入框草稿附件缩略栏与删除、大图灯箱预览及多图消息渲染 ✅ 2026-09-12
- [x] 优化侧栏会话状态标记：进行中改用 12px 柔灰 270° 大弧度慢速旋转圆环，待处理与未读圆点调为 7px，视觉沉静且轴心严格居中 ✅ 2026-09-12
- [x] 修复右侧面板显示失真：完成任务正确显示勾选与删除线（原判断值与引擎状态不符）、被拒绝或失败的修改不再计入文件变更、新建文件可查看写入内容、超大 diff 截断保护；面板底色改为与会话区一致、仅以一条竖线区分 ✅ 2026-09-12

## v0.1.3 (2026-09-12)

- [x] 青梧界面滚动条样式优化：参照 InkMark 设计，去除 Windows 原生粗重底轨，采用半透明圆角胶囊滑块与透明轨道，全域统一 12px（可视滑块 8px），日常默认态淡雅（0.24），悬停适度加深不过黑（0.38），会话区保持稳定槽位（scrollbar-gutter: stable） ✅ 2026-09-11
- [x] 青梧界面输入框与正文列宽收窄：首页输入框最大宽度调整为 700px；会话展开后输入框为 740px、正文列为 720px（两侧各内收 10px） ✅ 2026-09-11
- [x] 青梧界面工具调用行去卡片框：平铺无边框展示，图标与正文左缘对齐，展开详情靠缩进分层不再用分隔线，失败态由红色图标与状态文字示意；行字号提至 14px 靠近正文（辅助信息 13px） ✅ 2026-09-11
- [x] 青梧界面侧栏悬浮与选中底色对齐 Codex：改为与侧栏浅蓝同色相的半透明压暗（悬浮 3.5%、选中 5%），搜索、新会话、会话条目、工作区行统一换用侧栏同套悬浮底，深色模式同比例提亮 ✅ 2026-09-11
- [x] 青梧界面字体改为雅黑优先并统一正文字号：英数字不再走 Segoe UI、改用雅黑自带字形（标题栏同步），消息正文由 15px 调为 14px，与侧栏、输入框一致 ✅ 2026-09-11
- [x] 青梧界面侧栏字号与文字色调整：会话、工作区、「新会话」由 14px 升至 15px；侧栏文字改用柔灰（亮色 #3A3C3F，取 Codex 实测值），不再用正文黑 ✅ 2026-09-11
- [x] 侧栏开关上移标题栏菜单栏（对齐 ChatGPT 桌面版）：聊天区标题栏不再放开关按钮，侧栏展开时对话区改为浮在底色上的圆角面板；右侧工作区面板默认收起，需要时再展开 ✅ 2026-09-12
- [x] 修复与优化青梧界面视觉细节：清除拖拽条漏色蓝条与标题栏多余线条，恢复对话区左上角柔和圆角（12px 弧度），去除侧栏右侧多余硬分割线并改用柔和微阴影与色块自然界限，调淡顶栏分割线 ✅ 2026-09-12

## v0.1.2 (2026-09-11)

- [x] 输入框模型/推理强度/权限模式选择器：模型与强度走 session/modelCatalog + session/selectModel；权限会话内走 /permission 命令、空态写 settings permission.defaultPreset（新会话默认权限）；空态模型选择随建会话应用 ✅ 2026-09-11
- [x] 会话工作区绑定与上下文条：输入框上方或下方提供工作区 chip，可选择/新建工作区并随会话展示；移除侧栏「添加项目」单点入口，添加工作区统一收口到聊天框 ✅ 2026-09-11
- [x] 工作区分组新建会话：侧栏每个工作区分组下提供「新建会话」入口（对齐 dsh 口径） ✅ 2026-09-11
- [x] 侧栏工作区行样式：工作区行带文件夹图标（随展开/收起切换开合，不加折叠箭头）、可点击展开/收起会话列表，常态无单独底色，字体与会话条目一致，会话条目缩进与工作区名文字左对齐，悬停显示「···」操作菜单和新建会话按钮 ✅ 2026-09-11
- [x] 左右面板开关：侧栏与右侧工作区面板均有常驻按钮可收起/展开（dsh 口径「打开/收起侧边栏」，对齐 ChatGPT 桌面版交互） ✅ 2026-09-11
- [x] 侧栏视觉：侧栏底色改浅蓝并与标题栏左侧融合（对齐 ChatGPT 桌面版） ✅ 2026-09-11
- [x] 视觉对齐第二批：问候语/输入框区域淡径向光晕、输入框加大圆角与投影、模型与推理强度各占一个下拉且强度常显当前档、空输入时隐藏发送按钮（后于 2026-09-11 改为常显禁用态，见 Bug 修复）、权限与占位文案打磨、标题栏去居中标题 ✅ 2026-09-11
- [x] 思考过程改为默认折叠的单行摘要（对齐官方「思考」行）：思考块不再整段展开，折叠时只占一行高——左侧思考图标 + 「思考」，右侧灰字带出一行思考内容；思考进行中显示最后一行、右对齐跟随、行内扫光表示仍在推理，思考结束收起为第一行并右侧省略号；点这一行才展开完整思考。流式期间也按这个形态呈现（原先是思考中展开全文、正文开始才收起），在飞内容与回合结束后的落库布局一致，收束时不再跳位。待用户实机验证 ✅ 2026-09-11
- [x] 整轮过程折叠（对齐官方「已思考 · N 次工具调用」）：一轮收束后，答复之前的思考与执行（思考块、工具卡片、中间消息）收进一行过程行，只留最后那条正式答复；过程行为整行可点，左侧文字按内容给「已思考」或「N 次工具调用 · M 条消息」，右侧箭头表示展开态，点开把过程按原顺序铺出来（缩进 + 限高滚动），答复里的思考行也随之恢复显示。数据侧按日志的 turn 归属（turn/start、assistant/message、tool/call 都带轮次）把条目聚合成轮，`turn/end` 之前不收（运行中照旧逐条显示，不然会挡住正在跑的过程）；整轮只有过程没有答复时不收，避免点开是空的。代价：过程默认不可见，要看工具执行细节要点一下。待用户实机验证 ✅ 2026-09-11
- [x] 隐藏会话态的工作区 chip：会话开出后（引导页以外）输入框上方不再常显项目名/工作区选择器，归属改由侧栏分组查看；工作区 chip 只保留在新会话引导页，用于选新会话落点（代价：会话换工作区暂无常驻入口，收进下方会话菜单项）✅ 2026-09-11
- [x] 输入框上下文占用显示（对齐官方 ContextMeter）：发送按钮左侧常驻占用环，悬停显示「上下文已用 N%」、点击展开构成面板（系统提示词／工具定义／对话消息三段进度条 + 各自近似 token 数与「~已用 / 容量」）；数据取引擎的 `contextPressure`／`contextBreakdown` 投影（与官方 useProjection 同源同口径，分子优先 `projectedTokens`、缺采样或路线容量时整块不渲染），来源为 `session/list` 的投影列，并在会移动该投影的事件（request/header、request/context、assistant 结算、tool/result、system/message、compaction/*）到达时重取一次会话列表。官方是在客户端自己折投影，青梧直接复用宿主算好的结果，因此刷新粒度较官方粗（按事件而非按增量），够用且无需实现整套估算器。待用户实机验证 ✅ 2026-09-11
- [x] 会话正文列比输入框窄 32px（两侧各内收 16px）：原先两边同为 768px，现按官方口径「输入框 = 正文列 + 32px」改成 `--native-composer-w` / `--native-content-w` 两个变量；等待回答/授权的卡片与正文列等宽，队列条仍与输入框等宽。待用户实机验证 ✅ 2026-09-11
- [x] 运行中提交的消息在青梧界面无任何可见反馈：原因是提交走 `mode: "queue"` 后消息只进引擎 inbox，而界面只订阅日志事件、既无回显也无队列区。已实现：提交当帧插入本地回显（「发送中」），宿主落库或入队后自动退休；由 durable 事件 `agent/inbox/spliced` 折出待发送队列并在输入框上方常驻展示（「排队中」/「插话中」），行内支持编辑、删除、插话（走 `session/updateQueue`）；运行中草稿非空时同一位置改为「排队发送」按钮，不再只能靠 Enter 触发。待用户实机验证 ✅ 2026-09-11
- [x] 切换到正在运行的会话时右下角没有「停止」按钮：原因是 `api-session/status` 只在 running↔idle 跳变时发出，切换会话/重连不重放，而切换时界面无条件把运行态置为 false。已实现：切换会话时用 `session/list` 的 `Summary.running` 播种，并在每次列表刷新时对账兜底；空草稿显示「停止」、有草稿显示「排队发送」，不再出现右下角整块空掉 ✅ 2026-09-11
- [x] 青梧界面的审批/问答回执全部静默失败（真正的病根，也是「提问卡片点了没反应、切官方界面才答得上」的原因）：`DshBridge.eventResult` 发 `POST /api/$events/result` 时把载荷写成 `{ clientId, eventId, outcome }`，而网关 `parseRemoteEventResultPayload` 只接受「恰好一个 plain-object `args`字段」的载荷（`dsh-api-gateway` 第 922 行），于是请求被直接拒绝；桥又只把失败 `console.error` 到主进程日志，渲染层一无所知——卡片照旧消失、没有报错，宿主那边一直挂着等回答。已修：载荷改包 `{ args: {...} }`（与一元 RPC 同一信封规则），并把 `eventResult` 的返回改成 `DshRpcResult`，渲染层在失败时显示错误条、保留卡片不再假装成功（审批路径同样修好并加了 `clientId` 兜底）。已实机确认：青桐界面的提问回执能正常送达宿主 ✅ 2026-09-11
- [x] 输入框草稿不分会话：`input` 原本是全局单份状态，切换会话既不清空也不按会话保存，在会话 A 输入的文字会原样留在会话 B 的输入框里，回车即发到 B。已实现逐会话草稿（键为会话 id，无会话时用空串）：输入时即写入所属会话的草稿桶，切换会话载入目标会话的草稿，发出后清空、发送失败写回，会话被删除时清理该桶。仅存内存（重启不保留），如需「关掉应用草稿还在」再单独评估落盘 ✅ 2026-09-11
- [x] 提问卡片选择不可用且设计不达标：原因是选项都挤在同一行小按钮里、点完卡片既不消失也不清理本地状态（宿主只向其他客户端推 `cancel` 帧），重复点击被宿主静默忽略；且一次只提交单题答案，多题请求会拿到不完整回答。已重做：一题一区、纵向选项行（label + description，单选/多选标记）、整请求一次提交该请求下所有题目、每题可填「其他」自由文本（单选时与选中项互斥）、可「跳过此问题」（提交空 selected）、提交成功后卡片即时移除、失败保留并报错；识别 plan-review 意图并改为「计划审批」布局（计划 markdown 限高滚动 + 批准/拒绝按钮 + 「讨论一下」以 ASK_CANCELLED 拒绝等待，把控制权交回输入）；回执 `clientId` 为空时改取当前事件流身份，仍为空则明确报错而不是静默失败。已实机确认：青桐界面提交可正常送达宿主 ✅ 2026-09-11
- [x] 新建会话落在「无问候语、正文全空」的会话页：点「新会话」时 `createSessionIn` 会立即在宿主建出（或复用）一个空白会话并 `setCurrentId`，而引导页（居中问候 + 居中输入框）的渲染条件只看 `!currentId`，于是用户看到顶栏只有工作区名、正文一片空白、输入框贴底的页面——其他产品在第一条消息发出前都停留在新会话引导页。修复：引导页条件改为「未选中会话，或当前会话仍为 blank、无任何消息事件且未在运行」，发出第一条消息后自然切换为会话态 ✅ 2026-09-11
- [x] 空输入时看不到发送按钮：原因是 `.native-send.empty { visibility: hidden }`（自 2026-09-11 视觉对齐第二批起）把按钮整块藏掉，只有输入字符后才出现，用户找不到发送入口。改为常显禁用态：草稿为空时显示灰色圆钮（灰底 + 边框 + 灰箭头、不可点），有草稿后恢复蓝底可点，运行中空草稿仍是「停止」；已删除 `.empty` 规则与对应类名。待用户实机验证 ✅ 2026-09-11
- [x] 不断弹出终端窗口（v0.0.7 已修问题的回归）：根因是 GUI 子系统进程（Electron/electron.exe）不继承父进程控制台，而 dsh 用 `process.execPath` 逐层派生 Windows Job runner 与 windows-acl runner，整条链都没有控制台，最后那条 pwsh 只能自己新建一个可见控制台窗口（0.1.5-rc.1 引入新派生层后暴露，0.1.5-rc.2 仍未改变该派生方式；旧版每次派生自带 `windowsHide` 掩盖了它，主进程 `acquireHiddenConsole()` 的隐藏控制台也到不了引擎树）。改为用控制台子系统的官方 Node 运行时装包内启动引擎：版本与 Electron 内置 Node 对齐（24.18.1），`scripts/fetch-node-runtime.cjs` 按官方 SHASUMS256 校验后解出，`extraResources` 随包、`before-pack` 钩子保证就绪（失败即打包失败），引擎/runner/pwsh 因此全部继承主进程隐藏控制台；找不到运行时或主进程拿不到控制台时退回 Electron 方式。代价：安装包增大约 35 MB。待用户实际运行验证（开发态已实测：命令无控制台、不弹窗；打包态待发版验证） ✅ 2026-09-11
- [x] 引导页切换项目报「workspace/insertSessionBefore 失败：workspace/move-invalid…the session is not accounted」：原因是项目 chip 用该接口把当前会话「搬」到目标项目，而引擎的工作区归属在会话创建时按目录写死、该接口只受理已登记在本项目的会话，跨项目一律被拒（会话还是空的也一样）。已改为对齐官方口径——chip 切换的是新会话落点：在目标项目建/复用一个空白会话再切过去，未发送的草稿随人迁移，原来那个空白会话留在原项目、下次进入时复用；并移除已无引用的 workspace/insertSessionBefore 协议项 ✅ 2026-09-11
- [x] 青梧界面看不到流式输出、也看不到实时思考过程：引擎 0.1.5 起把「正在生成的回复」与「正在推理的思考」改为只在 follow 请求显式声明 `assistantStream: true` 时下发的过程内 live 帧（`assistant-stream`），旧版随日志落库的 `assistant/chunk` 已退化为历史格式（只剩 v0→v1→v2 迁移代码引用）；青梧既没声明该选项、也识别不了这类帧，于是文字要等整段结束才一次性出现，思考过程在回合进行中完全不可见（回合结束后「思考过程」折叠里仍能看到落库的推理）。已实现：follow 请求声明 `assistantStream: true`；处理 `assistant-stream` 帧（start 重置、chunk 增量、end 收束），推理增量实时进入「正在思考…」、文本增量实时进入正在生成的回复；revision 跳号（漏帧/重连）时自动重开一次流；重连开场基线里的压实增量（text-chunks / reasoning-chunks / tool-call-chunks）会重放，切回正在输出的会话不再空白。待用户实机验证 ✅ 2026-09-11
- [x] 输入框聚焦时描边变黑、投影加重：光标点进对话框，常态浅灰描边（`#e5e5e8`）会跳成近黑 `--native-fg` 并加重投影，硬黑线与浅灰常态反差过大；提问卡片的「其他（自行输入）」是同一写法（两处规则都早于 2026-09-11 界面改版，不是改版引入）。已删除两处聚焦样式，输入框描边与投影在光标进出时保持恒定。待用户实机验证 ✅ 2026-09-11
- [x] 提问/审批卡片在所有会话都显示、且一组多题铺满整屏：两个独立成因。①跨会话：`$events` 瀑布帧是全局事件流，帧上的 `agentId` 就是归属会话 id（引擎里 Agent id 恒等于 Session id），而青梧把待处理项存成两个不带会话归属的扁平数组，切换会话时只清对话、队列与草稿，于是卡片跟着人走进每个会话，看起来像每个会话都在等你回答。②占满屏幕：卡片把该请求下的所有题（引擎一次最多 4 题）纵向铺在同一个卡片里，每题都带选项列表 + 自由输入 + 跳过，卡片限高 60vh，同会话多个待处理请求还会继续往下叠。已按官方口径重做：待处理项带 `sessionId` 入库，渲染时只归到发起它的会话；子代理会话的请求归到它的根会话（青梧不展示子代理会话，否则子代理请求审批时卡片没有任何入口可答），归属会话已不存在的项兜底显示在当前会话（避免既不能提交也不能放弃）；同一会话只显示优先级最高的一条（计划审批 > 提问 > 授权，与官方一致），另有条目时在卡片下方说明还有几项；提问改为一题一屏 + 「第 n / N 题」进度 + 上一题/下一题（单选点选即翻下一题、下一题前须已作答或显式跳过、最后一题才「提交」）+「放弃整组问题」；侧栏会话行给出「等待回答／等待授权／计划待审」小圆点（等待状态优先于运行中圆点，对齐官方），另一个会话在等你时不再只能自己挨个点开去找。待用户实机验证 ✅ 2026-09-11
- [x] 输入框只能显示一行、多行输入要在框内滚动：`.native-composer-box textarea` 写的 `flex: 1` 等于 `flex-basis: 0%`，会盖掉自适应高度写的行内 `height`，输入框因此永远停在 `min-height`（44px ≈ 一行），输入框「随内容长高」那段代码等于没生效。已改为 `flex: 1 1 auto` 让高度重新说了算，最小高度按两行定为 58px（2 × 21px 行高 + 16px 上下内边距），空输入即显示两行、随内容长到 200px 上限后内部滚动；自适应高度同时改为按 value 统一拟合，切换会话载入草稿、发送后清空、发送失败写回都会重算。已用离屏渲染实测：空输入与两行均为 58px 无裁切，五行 121px，30 行 200px 封顶 ✅ 2026-09-11
- [x] 侧栏项目顺序会自己变：项目里新建会话或改项目名时，宿主会推一条该项目的最新快照（upsert），而青梧处理 upsert 时先把该项目从列表里摘掉、再追加到末尾，于是「刚在这个项目里新建了会话」的项目每次都掉到侧栏最下面——看起来就是项目顺序老在变；叠加 order 帧会按完整名单重建列表，名单里没有的条目会被直接丢掉。已改为：新项目插到最前面（与宿主「新建工作区前插」的落点一致，新建后位置也不再变），已在列表里的项目原地替换、顺序不动；order 帧改为按名次排序、名单外的工作区保留在原位。项目内的会话顺序本来就是登记的固定顺序，同样不随使用漂移。待用户实机验证 ✅ 2026-09-11

## v0.2.0 (2026-09-11)

- [x] 窗口位置记忆：关闭时记录位置、尺寸与最大化状态，下次启动还原；最大化记录还原边界，取消最大化可回到原位置；还原时校验显示器可见性，不可见回退默认尺寸居中。 ✅ 2026-09-11（src/main/window.ts、src/main/window-state.ts；npm run check 通过；窗口生命周期行为待用户实际体验）
- [x] userData 目录规范化：Electron 默认以 productName「青梧」作 userData 目录名，非 ASCII 路径在未来把路径传给引擎或第三方工具时有乱码隐患。新增 paths.ts 于其他模块求值前重定向到 %APPDATA%\qingwu，首次启动自动迁移旧目录中的自有配置（settings.json、window-state.json），Chromium 缓存留在旧目录重建；settings/window-state 路径改惰性求值，不依赖模块加载顺序；构建产物已核实重定向先于一切模块初始化执行。 ✅ 2026-09-11（src/main/paths.ts、src/main/index.ts、src/main/settings.ts、src/main/window-state.ts；npm run check 通过；迁移随本版本首次启动生效，待用户实际确认）
- [x] 关于页引擎版本号过期：手写 harnessVersion 字段随依赖升级漂移（显示 0.1.1-rc.2，实际 0.1.5-rc.1）。改为运行时读取 dsh 包 package.json 真实版本（覆盖 dev 与 asar/asar.unpacked 形态），读取失败显示「未知」，删除手写字段。 ✅ 2026-09-11（src/main/about.ts、src/main/config.ts；npm run check 通过）

## v0.0.9 (2026-09-08)

- [x] 自研界面（阶段 1：跑通主干）：主进程通信桥（fetch 转发 + 事件流转发）、界面骨架（会话列表、发消息、流式回复、审批卡）、「视图」菜单界面切换项；官方界面零改动。技术路线见 tech-architecture.md「UI 路线（2026-08-23 修订）」 ✅ 2026-08-23（npm run check 通过；RPC 信封格式已对真实引擎实测验证；界面交互待用户实际运行验证）
- [x] 发布工作流回写 CHANGELOG 必然非快进失败：CI 在 tag 检出上直接推 master，master 领先 tag 即被拒。改为切到远端 master 顶端重新生成后再推，消除结构性失败，仅剩秒级并发窗口。 ✅ 2026-08-22（.github/workflows/release.yml）
- [x] 标题栏菜单按 Esc / 再点按钮等关闭后高亮仍不消失（v0.0.8 实测）：popup 回调在这些无焦点变化的关闭路径下同样不触发，focus 兜底只在焦点转移时生效，此类路径全部无信号。改为 popup 处理器收拢至 menu.ts 并以 popupOpen 门控三路关闭信号（callback / webContents focus / 窗口 blur）；渲染层新增指针事件守卫，利用原生菜单开启期间页面收不到指针事件的特性，收到首个 mousemove/mousedown 即清除高亮；顺带移除粘滞状态下会引发幻影菜单的 onMouseEnter 悬停切换。 ✅ 2026-09-07（src/main/menu.ts、src/renderer/src/TitleBar.tsx；沙箱验证信号链路，类型检查与构建通过；真实手势待用户实际体验）
- [x] 升级 dsh 0.1.2-rc.1 后应用无法启动：新版引擎 Web 面强制 token 鉴权，就绪探测对根路径收到 401 即判未就绪，25 秒超时中止启动。改为按行解析引擎启动日志中的带 token Web 地址作为就绪探测与服务地址（解析失败退回裸地址以兼容旧版）；webview 由此携带 token 加载；主进程桥新增 token→cookie 握手并在 RPC POST 上携带。package.json 已恢复完整结构（scripts/devDependencies/build）并保留 0.1.2-rc.1 依赖；dsh-app-boot 的 junction 补丁已在上游重写修复，postinstall 移除。 ✅ 2026-09-08（src/main/harness.ts、src/main/dsh-bridge.ts、package.json；dev 启动实测就绪）
  - 遗留：自研桥对 0.1.2 的契约仍断裂——RPC 方法名全部 404（新 API 为 controller 形态）、events.mux/host 事件流无法连接（WS 携带 cookie+Origin 仍被挂断），官方界面不受影响（webview 自带 cookie 同源直连）。迁移工作归入下方「dsh 升级契约闸门」；期间原生自研界面的数据调用会失败，事件流重连日志每 3 秒报错属预期。
- [x] 自研桥迁移至 dsh 0.1.2 typert/gateway 契约（上一条遗留的落地）：一元调用为 POST /api/<endpoint>（如 session/list），载荷为具名参数包 {args} 信封；事件流统一为 /api/remote.mux 单条 WS（鉴权只认握手 cookie），桥内多路复用（open/cancel + item/error/end），断线自动重连并重放已开流；$events 逻辑流承载 api-session/* 通知与 approval/request、user-questions/request 瀑布，回执走 POST /api/$events/result；渲染层会话内容改用 session/follow 日志流（开场快照 + 实时事件），workspace.list 改由 workspace/follow 基线流承载，host.pickDirectory 改 directoryPicker/pick，session.prompt 增加 requestId。新增直接依赖 ws（remote.mux 握手需自定义 cookie 头）。 ✅ 2026-09-08（src/main/dsh-bridge.ts、src/renderer/src/native/*、src/shared/types.ts；wire 层已对独立引擎实测：session/list、session/create、session/prompt、session/cancel、workspace/follow、session/follow、$events ready 均返回 ok；各 endpoint 具名参数形态以 typert.remote-client.d.ts 为准——session/list 为 _request，其余带参端点为 request；界面交互待用户实际运行验证）

## v0.0.8 (2026-08-22)

- [x] 标题栏菜单点击外部空白关闭后按钮高亮不消失：Electron 原生菜单 popup 回调在"点击外部关闭"时不触发（electron#17341）。改为以菜单关闭后落点视图的 focus 事件作为确定关闭信号，通知标题栏清除高亮。 ✅ 2026-08-21（src/main/menu.js；待用户实际运行验证）
- [x] 自有代码迁移 TypeScript：主进程 / preload / 渲染层全量 .ts/.tsx，strict 类型检查纳入 npm run check 门禁，为后续自有插件对接 dsh 的 TS 类型契约打基础。 ✅ 2026-08-22（类型检查与构建通过；Windows 运行时行为无改动预期，待随下次实际使用回归）

## v0.0.7 (2026-08-22)

- [x] Windows 下 Agent 执行命令时不断闪现控制台窗口：Electron 主进程无控制台，dsh 子进程（pwsh 等）各自新建控制台窗口所致。方案：主进程启动时 AllocConsole 并立即 SW_HIDE 隐藏，让全部子进程继承该隐藏控制台；属青梧自有实现，不依赖 dsh 上游。 ✅ 2026-08-21（src/main/console.js，koffi 调用 Win32；待用户实际运行验证）

## v0.0.5 (2026-08-21)

- [x] 标题栏跟随系统明暗模式 ✅ 2026-08-21

## v0.0.4 (2026-08-21)

- [x] 标题栏与菜单栏整合（VSCode 风格自定义标题栏与原生 Snap Layout 贴靠） ✅ 2026-08-21
- [x] 系统托盘支持与关闭窗口最小化到托盘设置 ✅ 2026-08-21
- [x] 修复 Windows 任务栏右键菜单图标，绑定 AppUserModelId 并优化底层可执行文件名 ✅ 2026-08-21

## v0.0.2 (2026-08-20)

- [x] 优化生产打包配置，过滤 1.1 万个非运行时文件，解决 Windows 下解包卡顿耗时过长问题 ✅ 2026-08-20
- [x] 修复应用窗口菜单栏默认隐藏问题，调整为默认常驻顶部显示 ✅ 2026-08-20
- [x] 修复检查更新时 404 及底层网络报错直接弹窗问题，转为友好提示与脱敏信息 ✅ 2026-08-20
- [x] 清理关于对话框底部的多余文案描述 ✅ 2026-08-20
- [x] 更新应用桌面与窗口图标 ✅ 2026-08-20

## v0.0.1 (2026-08-20)

- [x] V0.0.1：Electron 桌面壳（拉起 dsh、加载官方 UI、干净启停、Windows 安装包） ✅ 2026-08-20
- [x] 启动后核实 dsh web 端口与参数、打包完整性（见 tech-architecture.md 待核实项） ✅ 2026-08-20
- [x] 原生菜单栏、关于对话框与手动检查更新机制（autoHideMenuBar、环境/引擎版本展示、InkMark 同款手动更新闭环） ✅ 2026-08-20
