青梧 待办

产品定位与边界见 [AGENTS.md](../AGENTS.md)；历史已完成事项见 [TODO-ARCHIVE.md](TODO-ARCHIVE.md)。

待办记录的任务不代表一定要做，是可以探讨的。如果实现过程中会带来更大的问题，或成本大于收益，必须提出来。

## 记录规则

1. 所有任务分为三类：新功能、Bug 修复、暂不考虑。
2. 新增任务一律追加到对应分类末尾。
3. 任务完成后标记 \[x]，并按 Obsidian Tasks 格式记录完成时间：✅ YYYY-MM-DD。已完成项说明保持简短。
4. 版本发布时，将已完成 \[x] 项统一剪切归档至 TODO-ARCHIVE.md 对应版本下。

## 新功能

- [ ] 会话分叉（Fork）：在助手回复操作栏放置分支图标，支持基于历史轮次分叉派生新会话（对接引擎 session/fork 接口），并支持从指定轮次或历史提问派生分支继续对话
- [ ] 助手回复操作栏重构：对齐官方 WebUI 紧凑横排布局，历史轮次鼠标悬停显现、最新轮次常驻显示，统合复制按钮与完成时间戳（如「9月20日 16:46」）
- [ ] Token 用量与用时指标面板：解析轮次收束事件中的消耗与耗时数据，展示「用量 X tok」与「用时 X」胶囊，点击弹出详情卡片查看模型路由、缓存命中率、各项 Token 明细及吐字速率（tok/s）、首字延迟（TTFT）
- [ ] 完成通知功能。
- [ ] 界面架构重构：将 NativeApp 巨型组件按职责拆分为引擎流订阅、滚动管理、会话操作、草稿管理、模型选择等 hooks 与共享组件（断连横幅、输入框包装），行为保持不变
- [ ] 客户端多语言（i18n）国际化：支持中英文界面语言切换，建立词条与工具语义映射机制，覆盖界面交互文本、工具调用卡片动词（如执行命令 / Run command、读取 / Read）及运行状态

- [x] 调整标题栏下拉菜单的圆角、间距、底色与投影 ✅ 2026-09-22
- [x] 调整输入框发送按钮尺寸与底色（32px → 28px、#3A83F7）并调整运行态停止方块（8px × 8px） ✅ 2026-09-22

## Bug 修复

- [x] 修复默认模型恢复引擎配置无效，以及默认模型、权限保存失败后显示与实际配置不一致的问题 ✅ 2026-09-22

- [ ] 修复并验证标题栏菜单反复出现的裁剪、偏移与预览不一致问题（包含键盘切换高亮、缩放定位、屏幕边界和焦点恢复）
  - 复发记录：`cef1df2` 已处理透明菜单窗口的闪烁、裁剪与尺寸跳动；2026-09-22 增加投影留白后，实际界面再次出现右侧、底部被裁切，且与隔离预览不一致。重复次数尚未完整核实。
  - 已确认的验证缺口：此前隔离预览自行处理窗口尺寸，绕过了正式 `MenuPopupManager`；数据测试和构建通过不能证明实际窗口尺寸、锚点与投影正确。
  - 已复现的裁切机制（2026-09-22）：在系统显示缩放 200%、菜单页面缩放 1 的环境，以旧 `MenuPopupManager` 搭配新菜单渲染代码运行，窗口为 180×231 DIP，面板为 180×221 DIP、左上偏移为 (16,16)，右侧超出窗口 16 DIP、底部超出 6 DIP；旧定位也未抵消新增投影留白。以配套新管理器运行，同一面板对应窗口为 212×253 DIP，四边完整保留 16 DIP。两组均运行正式管理器代码，不是此前自行计算尺寸的预览。
  - 恢复过程的证据边界：截图反馈之后，本任务没有继续修改菜单实现，只更新待办和执行诊断。23:31:58 留下配套新代码尺寸正常的诊断记录，23:36:51 留下旧管理器搭配新样式裁切的对照记录；曾观察到应用主进程于 23:30:49 重启。Git 记录另有 23:37:32 的工作区 reset、23:38:44 的其他模块提交；结合已说明的 stash 操作，菜单修改曾从工作区消失、后又恢复。没有采集截图时正在运行的主进程代码身份，也没有记录实际界面恢复正常的准确时点，因此尚不能确认原截图必然由版本混用造成，或指定某次重启/stash 恢复为实际恢复正常的直接事件。
  - 防复发要求：投影内边距、窗口宽高和锚点补偿必须配套变更。涉及主进程/preload 的改动，不能用前端热更新或单独样式预览代表整项修改已经生效。共享运行目录发生 stash、恢复或重新构建后，须重新核对工作区与运行版本；正常显示只记为现象恢复，未确认根因与生效过程时不得写成“排查中新增修复并闭环”。后续界面效果交实际使用确认，不擅自反复操作使用中的窗口。
  - 完成标准：使用正式菜单窗口链路，验证首次打开、关闭重开、长短菜单切换、方向键切换、两套界面、页面缩放及屏幕边缘；核对完整面板、四边投影和标题锚点，记录实测环境与未覆盖场景。不得只凭独立样式截图或模拟窗口测试勾选完成。
  - 解决后在对应技术模块说明中记录已证实的根因与防复发约束，本条收敛为简要完成状态后归档；归档不保留完整排障过程。

- [x] 修复发送失败覆盖其他会话或新编辑草稿、首次发送残留空白页草稿、历史翻页过期响应混入及会话流失败后加载状态不结束的问题 ✅ 2026-09-22
- [x] 修复鼠标在菜单栏向右移动无法触发相邻菜单展开的问题（将下拉弹窗顶部留白归零并紧贴标题栏下沿，解除对标题栏右侧项的物理遮挡） ✅ 2026-09-22

## 暂不考虑

- [ ] macOS / Linux 发行版
- [ ] 安装提速（首启动延迟解压 node\_modules）：安装包秒级装完、首次启动应用内解压带进度；总时长不变（杀软逐文件扫描成本仍在），治本依赖 dsh 上游 provider SDK 惰性化，上游变化后重新评估
