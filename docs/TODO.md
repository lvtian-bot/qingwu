# 青梧 待办

产品定位与边界见 product-positioning.md；历史已完成事项见 TODO-ARCHIVE.md。

待办记录的任务不代表一定要做，是可以探讨的。如果实现过程中会带来更大的问题，或成本大于收益，必须提出来。

## 记录规则

1. 所有任务分为三类：新功能、Bug 修复、暂不考虑。
2. 新增任务一律追加到对应分类末尾。
3. 任务完成后标记 [x]，并按 Obsidian Tasks 格式记录完成时间：✅ YYYY-MM-DD。已完成项说明保持简短。
4. 版本发布时，将已完成 [x] 项统一剪切归档至 TODO-ARCHIVE.md 对应版本下。

## 新功能

- [ ] 侧栏会话拖动排序与「手动排序 / 最近更新」切换：对齐官方（组内会话默认可拖动、排序方式可切换，并记忆用户顺序）。青梧现状是项目顺序与组内会话顺序都固定（先建的在上），侧栏不随使用漂移；哪天想调整顺序再做，需要引擎的 workspace/insertSessionBefore 支持与顺序落盘
- [x] 支持自定义模型供应商：新增/编辑/删除，配置接口地址与密钥，可从服务商拉取候选模型 ✅ 2026-09-20
- [x] 斜杠命令对齐官方体验：集成技能（Skill）与 skills/list 动态拉取、三小节分组与专属图标、中文别名与顿号容错、输入文字后词边界行内触发与精准消费、空白页自建会话执行 ✅ 2026-09-20
- [x] 输入框 @ 文件与目录引用对齐官方逻辑：支持 @ 与 @" 引号触发、调用引擎 fileReferences/list 动态检索文件、方向键导航与 Enter 采纳、Tab 键目录深入下钻 ✅ 2026-09-20
- [x] 默认界面模式切换为青梧界面：新配置默认以青梧界面启动，保留菜单切换与偏好记忆 ✅ 2026-09-20
- [x] 青梧界面增加引擎连接状态感知与断连兜底：实时展示断连横条与重连提示，支持主动重试 ✅ 2026-09-20
- [x] 运行中支持 Ctrl+Enter 插话发送：草稿非空时直接以 steer 模式立即生效，草稿为空且有排队项时批量插话发送全部排队消息（对齐官方体验） ✅ 2026-09-20

## Bug 修复

- [x] 完成回合的过程折叠未生效：修复 events.ts 中根据尚无 tier 的条目提前判断 foldable 的逻辑死锁，聚合单回合正式答复与过程条目，保留开场用户消息独立显示并补充折叠单元测试 ✅ 2026-09-20
- [x] 模型服务商选择器不再显示图标，移除 @lobehub/icons 依赖链（402 个包），安装包体积回落 ✅ 2026-09-20

## 暂不考虑

- [ ] 引擎重启后排队消息自动投递：评估后保持与官方一致（官方设计冷恢复保持停稳不自动唤醒，避免非预期扣费，由下次发送一并带出）
- [ ] 标题栏菜单打开后悬停切换菜单（自绘面板方案）：观感不佳已回退，仅保留未选中灰显；原生菜单模态特性导致该交互必须自绘，后续需完整自研方案再评估
- [ ] macOS / Linux 发行版
- [ ] 安装提速（首启动延迟解压 node_modules）：安装包秒级装完、首次启动应用内解压带进度；总时长不变（杀软逐文件扫描成本仍在），治本依赖 dsh 上游 provider SDK 惰性化，上游变化后重新评估
