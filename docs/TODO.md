# 青梧 待办

产品定位与边界见 product-positioning.md；历史已完成事项见 TODO-ARCHIVE.md。

待办记录的任务不代表一定要做，是可以探讨的。如果实现过程中会带来更大的问题，或成本大于收益，必须提出来。

## 记录规则

1. 所有任务分为三类：新功能、Bug 修复、暂不考虑。
2. 新增任务一律追加到对应分类末尾。
3. 任务完成后标记 [x]，并按 Obsidian Tasks 格式记录完成时间：✅ YYYY-MM-DD。已完成项说明保持简短。
4. 版本发布时，将已完成 [x] 项统一剪切归档至 TODO-ARCHIVE.md 对应版本下。

## 新功能

- [ ] 自研界面（阶段 3：默认切换 + 打磨）：默认打开自研界面、引擎断连等异常兜底
- [x] dsh 升级契约闸门：按当前 controller 公开约定检查界面消费类型与真实调用的具名参数，并验证基础响应信封与流帧；纳入 npm run check。原 host-apiproxy 方案已失效，动态事件内容与嵌套请求仍需对应行为测试及双界面验证 ✅ 2026-09-12
- [ ] 侧栏会话拖动排序与「手动排序 / 最近更新」切换：对齐官方（组内会话默认可拖动、排序方式可切换，并记忆用户顺序）。青梧现状是项目顺序与组内会话顺序都固定（先建的在上），侧栏不随使用漂移；哪天想调整顺序再做，需要引擎的 workspace/insertSessionBefore 支持与顺序落盘
- [ ] 设置页：应用内弹层，入口在侧栏底部，能力面对齐官方设置页并补青梧应用特有项；分区含通用（语言）、应用（关闭行为：退出/最小化到托盘、开机自启）、模型（提供方与 API 密钥管理，参照官方模型设置，走 settings/credentials API，密钥红脱不回显）、插件（清单与管理，走 dsh 插件设置 API）、Agent 预设、默认值（新会话默认权限与模型/强度，与输入框选择器空态逻辑联动）、关于（版本号只读展示）；引擎类设置读写走 dsh settings API 不自建存储，应用类设置存 Electron userData 由主进程管理；建议实施顺序：应用/默认值/关于 → 模型 → 插件 → Agent 预设

- [x] 按职责拆分青梧界面：输入、图片、问答、侧栏、消息展示与事件折算独立成模块，保留页面编排与现有交互；补充关键纯逻辑回归测试 ✅ 2026-09-12
- [x] 完善长期维护文档：更新当前通信架构、补充任务与源码导航、数据归属、开发排障与验证流程，区分历史方案和现行实现 ✅ 2026-09-12
- [x] 任务列表对齐官方停靠在输入框上方：从右侧面板剥离并迁移至输入框顶部，支持折叠与状态统计，右侧面板保留文件变更并预留后续标签页扩展 ✅ 2026-09-14
- [x] 支持在外部终端打开工作区：侧栏工作区操作菜单增加「在终端中打开」与「在文件管理器中打开」，顶部菜单与快捷键支持全局呼出，优先使用 Windows Terminal 并降级 PowerShell ✅ 2026-09-14

## Bug 修复

- [ ] 引擎重启后排队消息不会自动投递：队列项本身是 durable 事件，但引擎恢复会话时不唤醒队列，要再发一条消息才会被一起领走（界面现在会把队列显示出来，不会再「看不出问题」，但仍需决定是否做兜底）

- [x] 菜单与窗口焦点跟随当前界面：重新加载、缩放与开发者工具不再作用于隐藏的 DeepSeek 界面；本地页面导航按 origin 判断 ✅ 2026-09-12
- [x] 退出应用前等待引擎清理：重复退出共用清理过程，启动期间退出取消就绪等待，失败保留重试入口；安装更新前同样先清理引擎 ✅ 2026-09-12
- [x] 引擎启动日志按完整行解析，修复分块地址丢失，并对日志与报错中的鉴权地址脱敏 ✅ 2026-09-12
- [x] 修复会话权限切换使用过时参数：commands/execute 按当前引擎使用 submittedAttachments，并增加契约检查 ✅ 2026-09-12
- [ ] 完成回合的过程折叠未生效：events.ts 的 foldChatItems 在分配 answer/context 前，先根据尚无 tier 的条目判断 foldable 并跳过，导致完成回合仍没有正式答复与过程分组；最小事件序列为 turn/start → 含 reasoning/text 的 assistant/message → turn/end，需单独修复并验证折叠体验
- [x] 修复偶发启动超时与端口冲突排查困难：解耦就绪探测超时与轮询间隔，启动前校验端口占用，并持久化主进程脱敏日志至文件 ✅ 2026-09-14

## 暂不考虑

- [ ] 标题栏菜单打开后悬停切换菜单（自绘面板方案）：观感不佳已回退，仅保留未选中灰显；原生菜单模态特性导致该交互必须自绘，后续需完整自研方案再评估
- [ ] macOS / Linux 发行版
- [ ] 安装提速（首启动延迟解压 node_modules）：安装包秒级装完、首次启动应用内解压带进度；总时长不变（杀软逐文件扫描成本仍在），治本依赖 dsh 上游 provider SDK 惰性化，上游变化后重新评估
